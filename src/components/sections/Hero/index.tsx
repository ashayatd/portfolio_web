"use client";

import PortfolioPage from "./MainHeroPage";

export default function Hero() {
  return (
    <section className="relative h-auto ">
      <PortfolioPage />
      {/* <div className="sticky top-0 flex h-screen w-full overflow-hidden">
        <motion.div className="absolute inset-0 z-10 h-full w-full">
          <CityCanvas />
        </motion.div>
      </div> */}
    </section>
  );
}
