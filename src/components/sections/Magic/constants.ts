export const MAGIC_TITLE_WORDS = {
  building: "BUILDING",
  scalable: "SCALABLE",
  solutions: "SYSTEMS",
} as const;

export const MAGIC_SCRAMBLE = {
  initialMaxDelayMs: 300,
  intervalMs: 6000,
  resetDelayMs: 4000,
  buildingSpeed: 2000,
  scalableSpeed: 2300,
  solutionsSpeed: 2100,
  stagger: 100,
} as const;

export const MAGIC_COPY = {
  scrollLabel: "Scroll to explore",
} as const;
